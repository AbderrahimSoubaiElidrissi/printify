import Head from "next/head"

import Layout from "../components/Layout"; 

const Setting = () => {
  return <Layout>
     <Head>
        <title>Setting</title>
      </Head>
    Setting</Layout>;
};

Setting.propTypes = {};

export default Setting;
